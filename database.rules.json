{
  "rules": {
    "Users": {
      "$user_id": {
        
        ".write": "$user_id === auth.uid",
        ".read": "$user_id === auth.uid"
        
      }
    },
      "Answers_from_freshers": {
      "$user_id": {
        
        ".write": "$user_id === auth.uid",
        ".read": "$user_id === auth.uid"
        
      }
    }
  }
}
